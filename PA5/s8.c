int main(int m, int n){
	int x, y, x, k;
	
	while(x < 4) {
		if(x<0) break;
		switch(x){
		case 1: x++; break;
		case 2: x+=2; break;
		default: x--;
		}
		x += y;
	}
	printf(x, y, z, k, ++m, n--);
	k = 1.0;
	return w + v;
}
